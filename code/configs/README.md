# Configs

Store experiment configuration files here, such as YAML or JSON settings for:

- model architecture
- training schedules
- dataset paths
- rendering parameters

An example synthetic dataset-generation config is available at `dataset_generation.example.json`.

For future dataset adapters, keep dataset-specific paths and preprocessing settings in this folder rather than hard-coding them in scripts.
